/** Wix company site — Wix Bookings for 1:1 credit redemption lives here. */
export const WIX_SITE_ORIGIN = "https://www.provenunderpressure.com";

/** Default when `VITE_COACH_BOOKING_URL` is unset (local dev / preview). */
export const DEFAULT_COACH_BOOKING_URL = `${WIX_SITE_ORIGIN}/`;
