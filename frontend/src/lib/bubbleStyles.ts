/**
 * Thin bridge to Bubble CSS classes in @/styles (ir/slices/styles.json).
 * @deprecated Import from `@/styles` directly in new code.
 */
export {
  bubbleStyle,
  styleRef,
  BUBBLE_STYLE_IDS,
  type BubbleStyleRef,
} from "@/styles";
