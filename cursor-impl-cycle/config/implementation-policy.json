{
  "scope_focus": "authenticated_spa",
  "description": "DrSam authenticated user SPA (drsam-99657): auth/onboarding entry, shared shell and design system, then dashboard, journal, paths, chat, and settings. Public-only surfaces excluded.",
  "forbidden_write_paths": [
    "project/"
  ],
  "allowed_write_paths": [
    "frontend/",
    "adapter/",
    "cursor-impl-cycle/"
  ],
  "read_only_context_paths": [
    "project/",
    "project/supabase/",
    "ir/"
  ],
  "frontend_app_dir": "frontend",
  "adapter_dir": "adapter",
  "supabase_context_dir": "project/supabase",
  "bubble_api_replacement": {
    "strategy": "prefer_supabase_client",
    "policy_doc": "cursor-impl-cycle/docs/ADAPTER-POLICY.md",
    "adapter_when": [
      "Server secret required (service role, third-party API key) — not user JWT passthrough",
      "Inbound webhook or server-triggered side effect during Bubble migration",
      "Multi-step orchestration intentionally kept off the client (not Promise.all of public Supabase calls)",
      "Documented temporary migration facade with explicit remove-after note in decompose scope"
    ],
    "adapter_forbidden_when": [
      "Single edge function proxy with user JWT (use frontend fetch to functions/v1)",
      "Single Supabase RPC proxy (use supabase.rpc from frontend)",
      "Single RLS-backed REST read/write (use supabase.from from frontend)",
      "Decompose marks frontend+adapter without naming an allowed adapter_when reason"
    ],
    "forbidden": [
      "Duplicate or fork Supabase schema in adapter",
      "Modify project/supabase migrations or RLS",
      "Reimplement business rules that already exist in Supabase edge functions unless explicitly required by AC",
      "Add passthrough routes when an equivalent direct client call exists in the same module"
    ]
  },
  "app_ir_anchors": {
    "pages": ["AAL", "bTGNI", "bTGYf", "bTHDT", "bTHDa", "bTHDf", "bTHDV", "bTHDh"],
    "page_names": ["index", "onboarding", "reset_pw", "dashboard", "journal", "paths", "chat", "settings"]
  },
  "ui_fidelity": {
    "rubric_path": "cursor-impl-cycle/prompts/ui-fidelity-rubric.md",
    "bubble_source": "drsam-99657.bubble",
    "styles_slice": "ir/slices/styles.json",
    "require_data_bubble_id_markers": true,
    "preserve_element_hierarchy": true,
    "design_system_module": "MOD-DRSAM-DESIGN-SYSTEM"
  }
}
